# Compatibility pointer for the first release after templates became isolated.
# New Tart setups should copy templates/tart/.env.example beside that template:
#   cp templates/tart/.env.example templates/tart/.env
#
# An existing root .env is still read by Tart when templates/tart/.env is
# absent, with a deprecation warning. Libvirt never reads the root file.
